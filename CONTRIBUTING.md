# 笔记编写约定

## 目录与状态

- `docs/` 只放适合发布的内容；未核验、个人计划和临时材料留在 `drafts/`。
- 目录按知识领域划分；课程专属复习、作业和历年卷放在课程材料中，不作为通用概念的唯一位置。
- 新文件名使用小写 kebab-case；中文标题写在文档标题中，避免空格、括号和特殊符号进入新 URL。

## 文章模板

```markdown
---
title: 页面标题
status: draft # seed | draft | reviewed | evergreen
tags: [cs, c]
created: 2026-08-17
updated: 2026-08-17
sources:
  - https://example.com/
---

# 页面标题

## 结论
## 前置知识
## 正文
## 易错点
## 参考资料
```

每篇文章只能有一个一级标题。不要把“已校验”写成默认状态；只有对照教材、标准或原始资料完成核对后才使用 `reviewed`。

提交前运行 `make format` 自动统一标题层级、代码块标注、空行和尾随空格；再运行 `make check`，它会检查 Markdown 格式、未闭合代码围栏、公式并执行严格构建。

## 代码块

- 多行代码、终端命令和配置都使用成对的三反引号围栏，并标注语言，例如 ` ```c`、` ```bash` 或 ` ```text`。
- 围栏内的 `#` 是代码注释或预处理指令，不应缩进或移出围栏；未闭合的围栏会使后续标题错误地显示为代码。
- 展示包含反引号的 Markdown 示例时，外层围栏使用至少四个反引号。

## 数学公式

- 行内公式使用 `$...$`，独立公式使用 `$$...$$`；不要使用 `\(...\)` 或 `\[...\]`，也不要将公式分隔符写在代码块中。
- 网站使用仓库内置的 KaTeX 资源渲染公式，不依赖外部 CDN。新增复杂公式后运行 `mkdocs build --strict`，并在本地预览页面确认排版。

## 附件

- 优先把某篇笔记独有的资源放在同级 `assets/` 目录。
- 仅跨多篇笔记复用的资源放在 `docs/assets/<领域>/`。
- 使用小写 kebab-case 文件名，并写有意义的 alt 文本、来源与许可说明。
- 将原始可编辑文件与网站展示用的优化图分开；展示图优先使用 WebP 或 AVIF。
